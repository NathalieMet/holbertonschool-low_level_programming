#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "main.h"
/**
 * _strlen - returns the length of a string.
 * @s : variable
 * Return: void
 */

int _strlen(char *s)

{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
	if (i == 0)
		return (0);
	else
		return (i);
	}
}
