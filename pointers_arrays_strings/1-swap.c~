#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "main.h"
/**
 * swap_int - swaps the values of two integers.
 * @a : variable
 * @b : variable
 * Return: void
 */

void swap_int(int *a, int *b)

{
	int temporaire = 0;

	temporaire = *b;
	*b = *a;
	*a = *b;
}
