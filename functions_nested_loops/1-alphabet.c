#include "main.h"
/**
 * main -  prints _putchar, followed by a new line.
 * Return: Always 0.
 */

int main(void)
{
	char characters = 'a';

        for (characters = 'a' ; characters <= 'z' ; characters++)
        {
		_putchar (characters);
        }
        _putchar ('\n');
}
        return (0);
}q
